OCaml has automatic type inferencing. We will implement it later.

`let id x = x`
is a example of polymorphism (다헝타입)
('a) type

cons ::

custom data types:
`type days = Mon | Tue | ... | Sat | Sun;;
